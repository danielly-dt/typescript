function multiplicar(a: number, b: number): number {
    return a * b;
}

function saudacao(nome: string): string {
    return "Olá " + nome;
}

// Testes
console.log(multiplicar(5, 4)); // 20
console.log(saudacao("Danielly")); // Olá Danielly